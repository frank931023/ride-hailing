# 白癡 SA 作業

## 開啟步驟
1. `java --verison` 看自己有沒有安裝加進去PATH 沒有的話去裝
2. `mvn -v` 看自己有沒有安裝加進去PATH 沒有的話去裝
3. `mvn sprint-boot:run` 啟動

## 現在問題
11/24 - 啟動成功了 也顯示 `BUILD SUCCESS` 但是沒有看到 `http://localhost:8080/` 一坨大的
